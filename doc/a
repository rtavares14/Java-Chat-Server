GREEN - user login success
RED - error messages
BLUE - other user login
CYAN - user list
ORANGE - global message
WHITE - private message
YELLOW - user left
RED - error messages
PURPLE - server stuff
OLIVE - RPS
TEAL - File Transfer

TODO:
 # 1. Login
 # 1.2 Logout
 # 2. Broadcast a message
 # 3. Private message
 # 4. List users
 # 5. RPC
 # 6. Heartbeat
 # when leave randomly close ping pong timmer
 # ping
 # pong
 # hangup
 # pong Error
 # tests
 # 7. Bye
 # 8. Unknown command
 # 9. Error messages
 # close and open the server before each test
 # all tests from old protocol
 # 10. Test RPC
 # 11. Test private messages
 # 12. Test list
 # 13. Do Protocol File Transfer
 # 14. Received ok from protocol FileTransfer
    # 15. FileTransfer
 # send request
 # start timer
 # accept request
        # deny request
    #open thread to send file
    #start sending file
    # end sending file
    # 16. Test FileTransfer
    # all tests from new protocol
 # when unknown command is sent Parse error is recieved


 initialise the transfer
 store the file
 accept the file with the user that is sending



transfer to method that will connect the downloader and receiver
d- download
r- receive

need to have some kind of a way to know if the file is being sent or received


// Put both sender and receiver in the same file transfer session (i can have multiple file transfer sessions at the same time)
                        // I need to create one that will handle this file transfer session
                        // add the users as sender and receiver (roles R and S)
                        // add the file path, size and checksum
                        // create a new thread that will handle the file transfer
                        // start a timer for the receiver to accept or reject the file transfer
                        // if the receiver accepts the file transfer, the file transfer will start
                        // if the receiver rejects the file transfer, the file transfer will be cancelled
                        // if the receiver does not respond in time, the file transfer will be cancelled
